{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "dotnet restore && dotnet publish SmartCampus.API/SmartCampus.API.csproj -c Release -o /app/publish"
  },
  "deploy": {
    "startCommand": "cd /app/publish && dotnet SmartCampus.API.dll",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

